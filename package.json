{
  "name": "hive",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "npm run build --workspace @hive/shared && npm run build --workspace @hive/db && npm run build --workspace @hive/server && npm run build --workspace @hive/agent-runtime && npm run build --workspace @hive/web",
    "dev": "concurrently -n server,runtime,web -c cyan,magenta,green \"npm:dev:server\" \"npm:dev:runtime\" \"npm:dev:web\"",
    "dev:server": "npm run dev --workspace @hive/server",
    "dev:runtime": "npm run dev --workspace @hive/agent-runtime",
    "dev:web": "npm run dev --workspace @hive/web",
    "db:generate": "npm run db:generate --workspace @hive/server",
    "db:migrate": "npm run db:migrate --workspace @hive/server",
    "typecheck": "npm run typecheck --workspace @hive/shared && npm run typecheck --workspace @hive/db && npm run typecheck --workspace @hive/server && npm run typecheck --workspace @hive/agent-runtime && npm run typecheck --workspace @hive/web",
    "browser:install": "npx --yes @puppeteer/browsers install chrome-headless-shell@stable --path \"$HOME/.cache/puppeteer\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "puppeteer-core": "^25.3.0",
    "typescript": "^5.9.3"
  }
}
